spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exerc_security?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
